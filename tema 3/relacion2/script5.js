function parImpar(){
  var arr= Array;
  for(i=1;i<=100;i++){
    var añadir= arr.push(Math.random()*1001);
  }
  for(i=0;i=arr.length;i++){
    var resultado=(arr[i]/2);} 
  function comparar(a,b){
    if (a/2==0){
    return 1;
  }else{
    return -1;
  }
  }
  arr.sort(comparar);
}